# Supabase (required for the frontend). Copy to .env or .env.local and fill in.
# Security: The anon key is public by design - it is safe to expose in client-side code.
# Access control is enforced by Supabase Row Level Security (RLS) on your database tables,
# not by hiding this key. To rotate keys, use the Supabase Dashboard: Project Settings > API.
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
